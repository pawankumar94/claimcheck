{
  "name": "curated",
  "description": "Narrow, task-appropriate tool surface -- read-only, search-oriented. No Edit. Bash restricted via permission-rule prefix matching to read-only commands only.",
  "allowed_tools": "Read,Bash(grep *),Bash(find *),Bash(cat *),Bash(ls *),Bash(sed -n *)"
}
