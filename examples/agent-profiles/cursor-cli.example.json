{
  "name": "cursor-cli",
  "description": "UNVERIFIED TEMPLATE -- not tested against a live Cursor CLI (cursor-agent) install. Fill in the real flags for your installed version before use.",
  "command": "cursor-agent",
  "baseArgs": ["-p"],
  "promptPlacement": "after-base",
  "policyArgs": {
    "curated": [],
    "full": []
  },
  "extraArgs": ["--output-format", "json"],
  "output": {
    "type": "json",
    "resultField": "result"
  },
  "verified": false,
  "verificationNote": "TODO before use: (1) confirm the binary name and non-interactive flag against `cursor-agent --help` on your machine -- this may not match your installed version; (2) confirm whether --output-format json exists and what field actually carries the answer text (resultField below is a guess, not confirmed); (3) find the flag that restricts tool/file access, if any, and map curated vs full policyArgs to it. Rename this file to cursor-cli.json (drop .example) once verified, and flip \"verified\" to true with a note on how you checked."
}
